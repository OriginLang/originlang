# Vendored third-party sources and patches.
# External dependencies fetched via Bazel rules are declared in WORKSPACE;
# this directory holds anything vendored/copied directly into the repo.

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "third_party",
    srcs = [],
)
