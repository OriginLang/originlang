# TypeScript framework core.
# Placeholder until real TS source + aspect_rules_ts toolchain are wired up.
# Once TS sources exist, replace this filegroup with:
#   load("@aspect_rules_ts//ts:defs.bzl", "ts_project")
#   ts_project(name = "core", srcs = glob(["core/**/*.ts"]), tsconfig = "//:tsconfig")

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "core",
    srcs = glob(["core/**/*.ts"], allow_empty = True),
)
