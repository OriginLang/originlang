# Executable entry points (CLI tools, standalone programs).
# Each app lives in its own subdirectory, e.g. //apps:my_app

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "apps",
    srcs = [],
)
