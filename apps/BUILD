# Product-facing executable entry points (CLI tools and standalone programs).
# Shared runtime and SDK code must remain outside this directory. Each app owns
# a subdirectory and declares its own target when implementation is added.

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "apps",
    srcs = [],
)
