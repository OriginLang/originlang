# Diagram of tests. Per-language unit and integration tests live in
# tests/{java,cpp,rust,python,ts}/.

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "tests",
    srcs = [],
)
