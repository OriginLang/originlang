# Deployable network services / daemons.
# Each service lives in its own subdirectory, e.g. //services:api_server

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "services",
    srcs = [],
)
