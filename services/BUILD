# Deployable network services and daemons.
# A service composes runtime/ libraries; it must not become a second runtime.
# Each service owns a subdirectory and declares its own target when added.

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "services",
    srcs = [],
)
