# Runtime

运行时核心。这里放置所有宿主都需要的、与具体插件语言无关的基础能力。

| 目录 | 职责 |
| --- | --- |
| `core/` | 生命周期、执行上下文和版本等稳定模型。 |
| `services/` | 调度、权限、存储等由运行时协调的系统服务。 |
| `ipc/` | JSON-RPC 协议及 stdio、TCP 等传输实现。 |
| `host-api/` | 由宿主向插件公开的 API 契约。 |

运行时不能依赖某个具体 SDK、官方插件或示例；语言绑定应放在 `sdk/`，执行载体适配应放在 `adapters/`。
